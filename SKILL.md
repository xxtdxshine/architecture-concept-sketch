---
name: architecture-concept-sketch
description: 将建筑照片或效果图分析并抽象为大师式自由涂写概念稿，分别用单一钢笔与炭笔绘制主稿和两幅构成小图，择优与原图拼成3:4竖版，仅交付三张图片。用于建筑大师手绘、建筑照片转概念草稿、建筑构成抽象；不用于写实描图、彩色建筑插画或普通摄影抽象海报。
---

# 建筑概念手绘

输入一张建筑照片/效果图，交付上方完整原图、下方米白手绘的 1800×2400 PNG，以及两张独立手稿（钢笔稿、炭笔稿）。仅交付这三张 PNG。默认高度抽象、接近盖里自由涂写的未完成草稿。每组画痕必须对应输入中的构成关系。

先读 [用户认可的风格标尺](references/approved-style.md)，按直线或曲面类型查看其中一张下方手稿；这两张选稿比“第一版”的版本称呼更准确地界定目标画风。照片提供构成起点，不以轮廓相似度作为首要验收标准。

## 先分析再画

1. 用 `view_image` 查看输入。拼图未指定目标时询问使用哪个照片区域，不能把草图当作照片。支持 Pillow 可读静态图；透明图以米白承底并说明；不默取动画第一帧。无建筑或主体不清时要求明确输入。
2. 提取主要体量及重心、1—3 个构成动作、虚实/场地关系、2—4 个辨识锚点。仅依据可见信息，删除多数窗格、材料缝、反光、车辆和装饰；不推断隐藏平面或真实设计过程。
3. 读 [风格路由](references/style-routing.md)，选择一种主表达方法，分别生成钢笔稿和炭笔稿；钢笔按用户认可的两张样稿校准；炭笔必须查看 references/images/隈研吾.jpg，按其中宽厚侧锋、颗粒、黑白留空和宽窄连续变化转译，详见风格标尺。两种画材均保留自由构想，不要求照片轮廓相似。不能因参考图偏黑就断言原作者用了炭笔。
4. 读 [参考图目录](references/reference-catalog.md) 中相关条目，并用 `view_image` 查看 `references/images/` 中对应图片，通常 1—2 张。输入决定内容，参考只提供画痕和抽象方法。作品事实见 [来源](references/sources.md)，未知归属保持未知。

## 内容和媒介

- 分别生成单一钢笔稿和单一炭笔稿，每稿的主稿、小图、辅助线画材一致。画痕只黑及自然灰阶，米白纸底除外。禁用彩色、水彩、马克笔、毛笔、墨水晕染与跨画材底稿。
- 主稿约占手绘内容 70%，延续照片大致视角，允许形变、断线、回环与试探复线，锚点保留动作与张力即可；可夸张比例、压缩体量、局部移位、省略边界，不要求与照片轮廓一一对应。不把输入替换为无关建筑。
- 固定两幅小图：体量分组；主要构成动作或虚实/场地关系。两图有不同用途，不是主稿缩小复制，不虚构内部平剖面。
- 默认无文字、签名、日期、标题、水印、边框、器材摆拍。密集描窗、自动边缘滤镜、满幅素描、无关装饰曲线均不合格。

## 生成与拼版

1. 使用 Python 3 + Pillow。运行 `python scripts/compose.py layout --photo INPUT` 获取下区尺寸；脚本路径相对于本 skill 目录。
2. 按 [生成模板](references/generation.md) 分别填写两种画材的提示词，分析与提示词仅用于内部工作。内置 `image_gen` 只生成下方手绘区，宽高比接近布局结果；照片为内容输入，参考为画痕输入，明确编号与角色。调用前已经查看全部引用图。不能把照片上区一起交给 AI 重绘。内置工具不可用则报告阻碍，未经用户指定不改用 CLI/API。
3. `view_image` 检查原始手绘，每张首次后最多两轮定向修正。混合媒介不能靠灰度化冒充通过。保留原始生成文件，使用工具实际返回路径；无可读路径时先解决文件可用性，不虚构路径或输出参数。
4. 本流程已获用户授权使用程序缩放、纸底规范化与拼版。运行 `python scripts/compose.py compose --photo INPUT --sketch RAW_SKETCH --output-dir WORK --name MEDIUM`。下区规范为黑灰画痕叠加 `#F5F1E8` 米白，不改变线条几何；明显彩色/混合画材必须先重生。照片仅还原 EXIF 方向并等比例缩放，不裁切、拉伸或调色。透明照片承米白并说明。自动版本化，不覆盖输入。
5. 保留已确认的原图保护版式：3:4 画布默认 1800×2400，原图从左上角完整等比例通栏铺满宽度，顶部及左右无边框，不裁切、不调色。照片高度按其比例计算，下区才是米白，直接衔接无间隔。下区完整容纳草图不拉伸。极高照片导致下区不足15%时依脚本提示向用户明确版式选择。
6. 运行 `python scripts/compose.py verify --manifest WORK/MEDIUM-vN.json`，验证尺寸、缩放照片像素完全一致、源文件哈希、下区与纸色单色范围。技术验证不证明画材或概念质量，必须再次查看最终成图。
7. 两稿分别在交付目录外的临时 WORK 目录运行拼版与验证。按构成表达和画面适配选择更合适的一稿用于最终拼合。只将选中拼合图与两种规范化独立下区复制到新的版本交付目录，分别命名“成图.png”“钢笔稿.png”“炭笔稿.png”；所选独立稿与拼合下区像素一致。目录严格只有三张图片，不交付提示词、记录、JSON、原始图或额外预览。旧稿不覆盖、不删除。失败项诚实标明。
8. 最终回复给三张图片的绝对路径链接和一句简短说明即可。双稿交付不改变第一版的风格、参考路由、主稿与两小图关系。
## 验证 skill

运行 skill-creator 的 `quick_validate.py`；脚本回归运行 `python scripts/test_compose.py`。视觉验收必须真实调用生成工具：原图完整，单一媒介，1 主稿+2 小图，动作来自照片，足够疏松自由，无伪造签名。样图保留工作区，不装进个人 skill。



